{% extends 'base.html.twig' %}
{% block body %}
    {%  include 'menu.html.twig' %}
    <div class="login_view register_view">
        <h2>Rejestracja</h2>
        {{ form_start(form) }}
            {{ form_widget(form) }}
            <button type="submit">{% trans %}Zarejestruj{% endtrans %}</button>
        {{ form_end(form) }}
        <span>Posiadasz konto? Zaloguj sie <a href="{{ path('login') }}"><strong>tutaj</strong></a></span>
    </div>
{% endblock %}