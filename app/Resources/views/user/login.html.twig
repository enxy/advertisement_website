{% extends 'base.html.twig' %}
{% block body %}
     {% include 'menu.html.twig' %}
    {% if error %}
            <div>{{ error.messageKey|trans(error.messageData, 'security') }}</div>
    {% endif %}
        {{ form_start(form) }}
        {{ form_widget(form) }}
        <button type="submit">{% trans %}Login{% endtrans %}</button>
        {{ form_end(form) }}
        <span>Nie posiadasz konto? Zarejestruj sie <a href=" {{ psth('rejestracja'}} ">tutaj</a></span>

{% endblock %}